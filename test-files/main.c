#include <stdio.h>

#define HELLO(sd) printf(#sd)
#define HI 6

int main() {
    const int i = 0;
    int s[] = {  };
label:
    char * hmm = "hel\nlo";
    HELLO(5);
}

// https://some.link.com

typedef enum Hi Hi;

enum Hi {
    HMM
};

struct Hello {

};
